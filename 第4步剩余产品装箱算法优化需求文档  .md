# 第4步剩余产品装箱算法优化需求文档

# 第4步「剩余产品装箱与定制箱建议」算法优化需求文档

## 1. 背景问题

当前第4步在分析剩余产品时，输出逻辑过于粗糙。

例如系统看到某些 SKU 剩余数量较多，就直接输出：

> 剩余产品较多，建议定制箱子。
> 

这个建议没有实际执行价值，因为它没有回答以下关键问题：

- 这个 SKU 应该用多大的箱子？
- 一箱应该装几个？
- 产品应该平放、立放，还是旋转摆放？
- 是否可以在 60cm 单边限制内装完？
- 是否需要考虑纸箱内径和外径的差异？
- 是否可以和软包装小件混装？
- 是单 SKU 专用箱，还是混装箱？

因此，第4步需要从“泛泛建议定制箱子”，升级为“基于 SKU 尺寸、剩余数量、包装形态，自动计算可执行装箱方案”。

---

## 2. 核心目标

第4步的核心目标不是简单提示“定制箱子”，而是：

> 基于每个 SKU 的产品尺寸、剩余数量和包装类型，在外箱单边最长不超过 60cm 的限制内，为每个 SKU 找到尽可能合理的装箱方案，并优先处理硬包装大件，再利用剩余空间混装软包装小件。
> 

最终输出应包含：

- 推荐箱型尺寸；
- 单箱装入数量；
- 箱数；
- 摆放方向；
- 是否满足 60cm 限制；
- 是否可以混装；
- 混装建议；
- 计算依据；
- 风险提示。

---

## 3. 重要业务理解

### 3.1 箱子是为了把 SKU 合理装完

例如 SKU：`BR-ICE-DRAGON-FBA`

假设产品尺寸为：

```
长：43.2cm
宽：33cm
高 / 厚度：8.5cm
```

如果剩余数量是 6 个，那么定制箱子的目的应该是：

> 在 60cm 单边限制内，尽可能设计一个箱子，把这 6 个产品刚好装完。
> 

而不是笼统说“建议定制箱子”。

### 3.2 产品尺寸不能直接等于外箱尺寸

产品实际占用的是纸箱的**内径空间**。

但采购纸箱时，通常看到的是纸箱**外径尺寸**。

所以不能直接用：

```
产品占用尺寸 = 外箱尺寸
```

而应该考虑：

```
外箱尺寸 > 内部实际需求尺寸
```

也就是说，需要给纸箱厚度、内外径差异、装箱余量留空间。

例如：

```
产品底面：43.2 × 33cm
6个叠放高度：8.5 × 6 = 51cm
理论内部需求：43.2 × 33 × 51cm
```

但推荐外箱不能直接写：

```
43.2 × 33 × 51cm
```

而应该写成类似：

```
推荐外箱约：44.5 × 34.5 × 53cm
```

前提是：

```
最长边 ≤ 60cm
```

并且内部空间确实能装下产品。

### 3.3 优先处理硬包装 SKU

产品应分为两类：

1. **硬包装 / 不可压缩 SKU**
    - 尺寸大；
    - 不能挤压；
    - 不好混装；
    - 通常决定箱子尺寸；
    - 应优先设计专用箱或主箱型。
2. **软包装 / 可轻微压缩 SKU**
    - 尺寸小；
    - 可作为填充件；
    - 适合放进硬包装箱子的侧边、顶部或空余空间；
    - 如果无法混装，再单独合箱。

因此算法顺序应该是：

```
先处理硬包装大件 → 设计主箱型 → 检查剩余空间 → 再尝试混装软包装小件
```

---

## 4. 算法整体流程

### Step 1：读取 SKU 数据

每个 SKU 必须读取以下字段：

```
SKU 名称
剩余数量
产品长
产品宽
产品高 / 厚度
产品重量，如有
包装类型：硬包装 / 软包装
是否易碎
是否可混装
是否有特殊限制
```

如果字段缺失，需要标记：

```
缺少尺寸 / 重量数据，当前方案仅为估算。
```

### Step 2：SKU 优先级排序

处理顺序建议如下：

```
1. 硬包装 SKU 优先
2. 大尺寸 SKU 优先
3. 剩余数量多的 SKU 优先
4. 软包装 SKU 最后处理
```

原因：

- 硬包装尺寸大，不能压缩；
- 大件 SKU 决定主要箱型；
- 软包装小件可以作为填充产品，不应优先占用箱型设计。

### Step 3：枚举产品摆放方向

对每个硬包装 SKU，不能只默认一种摆放方式。

假设产品尺寸为：

```
L = 长
W = 宽
H = 高 / 厚度
```

需要枚举以下摆放方式：

#### 方案 A：平放叠加

```
箱子底面 = L × W
叠加方向 = H
```

适用于薄片状、盒装、板状产品。

例如：

```
产品尺寸：43.2 × 33 × 8.5cm
一箱装 6 个
内部高度需求 = 8.5 × 6 = 51cm
```

#### 方案 B：立放叠加

```
箱子底面 = W × H
叠加方向 = L
```

或：

```
箱子底面 = L × H
叠加方向 = W
```

适用于立起来更节省空间，且产品不易损坏的情况。

#### 方案 C：旋转摆放

允许系统尝试长宽高的不同排列组合：

```
L × W × H
L × H × W
W × L × H
W × H × L
H × L × W
H × W × L
```

每种组合都需要判断：

```
推荐外箱最长边是否 ≤ 60cm
单箱可装数量
是否可以刚好装完剩余数量
空间利用率
是否适合混装
```

---

## 5. 箱子尺寸计算逻辑

### 5.1 内径需求计算

对于某个摆放方案：

```
底面长 = A
底面宽 = B
叠加方向尺寸 = C
单箱装入数量 = N
```

则内部空间需求为：

```
内径需求长 = A
内径需求宽 = B
内径需求高 = C × N
```

### 5.2 安全余量

由于产品不能刚好卡死，需要预留装箱余量。

建议默认：

```
硬包装产品：每个方向预留 0.5-1.5cm
软包装产品：每个方向预留 0.3-1cm
易碎产品：需要额外缓冲空间
```

因此：

```
修正后内径长 = 内径需求长 + 长方向安全余量
修正后内径宽 = 内径需求宽 + 宽方向安全余量
修正后内径高 = 内径需求高 + 高方向安全余量
```

### 5.3 外径修正

由于采购纸箱通常看外径，而产品占用的是内径，需要进一步修正。

如果纸箱厚度未知，可以默认：

```
外径长 = 修正后内径长 + 0.5-1.5cm
外径宽 = 修正后内径宽 + 0.5-1.5cm
外径高 = 修正后内径高 + 0.5-1.5cm
```

最终判断：

```
max(外径长, 外径宽, 外径高) ≤ 60cm
```

如果不满足，则需要：

```
减少单箱装入数量
或切换摆放方向
或拆成多箱
```

---

## 6. 单箱最大装入数量计算

在 60cm 单边限制下，需要计算每个摆放方案的最大装入数量。

伪公式：

```
可用最大外径 = 60cm

可用内部叠加空间 ≈ 60cm - 外径修正 - 安全余量

最大可叠加数量 = floor(可用内部叠加空间 / 单个产品叠加方向尺寸)
```

但实际推荐时，不应只追求最大数量，还要结合剩余数量。

---

## 7. 装完优先级规则

系统选择方案时，应按以下优先级：

```
1. 能一箱刚好装完当前 SKU
2. 能用整数箱装完当前 SKU
3. 总箱数最少
4. 空箱率最低
5. 最长边不超过 60cm
6. 箱型稳定，方便实际装箱
7. 有利于混装软包装 SKU
```

---

## 8. 示例：BR-ICE-DRAGON-FBA

### 输入数据

```
SKU：BR-ICE-DRAGON-FBA
产品尺寸：43.2 × 33 × 8.5cm
剩余数量：6个
包装类型：硬包装
限制：外箱最长边 ≤ 60cm
```

### 摆放方式：平放叠加

```
底面尺寸 = 43.2 × 33cm
叠加方向 = 8.5cm
单箱数量 = 6
```

计算：

```
内部高度需求 = 8.5 × 6 = 51cm
理论内部需求 = 43.2 × 33 × 51cm
```

考虑安全余量和内外径差异：

```
推荐外箱约 = 44.5 × 34.5 × 53cm
```

判断：

```
最长边 = 53cm
53cm ≤ 60cm
符合限制
```

输出建议：

```
建议为 BR-ICE-DRAGON-FBA 定制约 44.5 × 34.5 × 53cm 的外箱。
采用平放叠加方式，一箱装 6 个，可一箱装完。
该方案最长边约 53cm，未超过 60cm 限制。
由于箱内空间利用率较高，原则上不建议再混装大件产品；如有极小软包装 SKU，可评估是否放入顶部或侧边余量。
```

---

## 9. 混装算法逻辑

### 9.1 混装前提

只有在硬包装 SKU 的主箱型确定后，才检查是否可以混装软包装 SKU。

软包装 SKU 可以作为填充对象，但必须满足：

```
1. 尺寸能放入剩余空间
2. 可轻微压缩
3. 不会压坏硬包装产品
4. 不会造成外箱鼓包
5. 混装后不超过重量限制
6. 混装后最长边仍然 ≤ 60cm
7. SKU 之间不会互相损坏
8. 发货规则允许混装
```

### 9.2 剩余空间判断

对于每个硬包装主箱型，需要估算：

```
箱内剩余长方向空间
箱内剩余宽方向空间
箱内剩余高方向空间
顶部剩余空间
侧边剩余空间
```

然后判断软包装 SKU 是否可以放入。

### 9.3 混装输出示例

如果可以混装：

```
建议以 BR-ICE-DRAGON-FBA 为主箱：
- BR-ICE-DRAGON-FBA：6个
- 可利用顶部 / 侧边空间混装 XXX 软包装 SKU：X个
- 推荐外箱尺寸：44.5 × 34.5 × 53cm
- 混装后最长边仍小于 60cm
- 注意不要强行塞入，避免外箱鼓包或压坏硬包装产品
```

如果不适合混装：

```
该箱型主要空间已被 BR-ICE-DRAGON-FBA 占用，剩余空间较小，不建议混装。
软包装 SKU 建议另行合箱处理。
```

---

## 10. 输出格式要求

第4步中，每个 SKU 的建议必须按以下格式输出：

```markdown
### SKU：XXX

- 剩余数量：
- 产品尺寸：
- 包装类型：硬包装 / 软包装
- 推荐装箱方式：平放叠加 / 立放叠加 / 旋转摆放 / 混装
- 单箱装入数量：
- 预计箱数：
- 理论内部需求尺寸：
- 推荐外箱尺寸：
- 是否满足 60cm 单边限制：
- 是否建议单 SKU 专用箱：
- 是否可混装：
- 可混装 SKU：
- 计算依据：
- 风险提示：
```

---

## 11. 禁止输出的低质量结果

禁止只输出：

```
剩余产品较多，建议定制箱子。
```

禁止只输出：

```
建议使用大箱子混装。
```

禁止只输出没有计算过程的建议：

```
建议定制 60cm 箱子。
```

---

## 12. 正确输出示例

应该输出类似：

```markdown
### SKU：BR-ICE-DRAGON-FBA

- 剩余数量：6个
- 产品尺寸：43.2 × 33 × 8.5cm
- 包装类型：硬包装
- 推荐装箱方式：平放叠加
- 单箱装入数量：6个
- 预计箱数：1箱
- 理论内部需求尺寸：43.2 × 33 × 51cm
- 推荐外箱尺寸：约 44.5 × 34.5 × 53cm
- 是否满足 60cm 单边限制：满足，最长边约 53cm
- 是否建议单 SKU 专用箱：是
- 是否可混装：不优先建议混装，仅可尝试放入极小软包装 SKU
- 计算依据：
  - 产品平放时底面为 43.2 × 33cm；
  - 单个产品厚度为 8.5cm；
  - 6个叠放高度为 8.5 × 6 = 51cm；
  - 考虑纸箱内径、外径差异和装箱余量，推荐外箱放大至约 44.5 × 34.5 × 53cm；
  - 最长边 53cm，小于 60cm 限制。
- 风险提示：
  - 采购纸箱前需确认供应商标注的是内径还是外径；
  - 如果产品硬包装不能受压，需要预留更多缓冲空间；
  - 如果实际外箱厚度较大，需重新校验内径是否足够。
```

---

## 13. 软包装 SKU 处理规则

软包装 SKU 不应一开始就单独设计箱子。

优先顺序：

```
1. 先尝试填充进硬包装 SKU 主箱型的空余空间
2. 如果不能混装，再将多个软包装 SKU 合并成软包装混装箱
3. 如果仍无法合并，再单独小箱处理
```

软包装 SKU 输出示例：

```markdown
### SKU：XXX-SOFT-PACK

- 剩余数量：12个
- 产品尺寸：10 × 8 × 2cm
- 包装类型：软包装
- 推荐装箱方式：优先作为混装填充件
- 优先混装箱型：BR-ICE-DRAGON-FBA 主箱
- 可混装数量：预计 X 个，需根据实际箱内顶部 / 侧边空间确认
- 如果无法混装：建议与其他软包装 SKU 合并成小箱
- 风险提示：不能强行塞入导致外箱鼓包
```

---

## 14. 最终输出需要包含汇总表

第4步最后应输出一个汇总表：

```markdown
| 箱型 | 主 SKU | 混装 SKU | 推荐外箱尺寸 | 单箱装入数量 | 箱数 | 最长边是否 ≤60cm | 备注 |
|---|---|---|---|---|---|---|---|
| 箱型 A | BR-ICE-DRAGON-FBA | 无 / 少量软包装 | 44.5 × 34.5 × 53cm | 6个 | 1箱 | 是 | 单 SKU 专用箱 |
| 箱型 B | XXX | YYY | XX × XX × XXcm | X个 | X箱 | 是 | 混装箱 |
```

---

## 15. 核心开发要求总结

请开发时重点实现以下能力：

```
1. 读取每个 SKU 的尺寸、数量、包装类型
2. 区分硬包装和软包装
3. 优先为硬包装 SKU 设计箱型
4. 枚举产品摆放方向
5. 计算不同摆放方式下的单箱最大装入数量
6. 区分纸箱内径和外径
7. 自动加入安全余量
8. 校验最长边是否超过 60cm
9. 优先选择刚好装完或整数箱装完的方案
10. 计算箱内剩余空间
11. 判断是否可以混装软包装 SKU
12. 输出具体箱规、装箱数量、箱数、计算依据和风险提示
13. 禁止输出没有计算依据的空泛建议
```

---

## 16. 一句话需求定义

第4步需要从：

```
剩余产品较多，建议定制箱子。
```

升级为：

```
根据每个 SKU 的产品尺寸、剩余数量和包装形态，在 60cm 单边限制内，自动计算单 SKU 专用箱或混装箱方案，并输出推荐外箱尺寸、装箱数量、箱数、摆放方向、混装可能性和完整计算依据。
```