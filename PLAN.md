# FBA 装箱优化工具 — 实施计划（修订版）

## 背景

亚马逊卖家发货时需将产品分配到5箱以上以减免入库配置费。卖家有多种规格箱子（如60×60×60cm、60×50×50cm、60×40×50cm）和多个SKU，需计算最优混装方案。箱子外径和内径差约2cm。项目仅有一个亚马逊导出的xlsx模板，需从零构建工具。

**关键需求：**
1. **混装是必备功能** — xlsx中箱子13-17混装了4个SKU（潜艇7+双层保险柜12+重型保险柜12+单层保险柜5=36件/箱），这是常态
2. **软硬包装区分** — 软包装（如糖果袋）设压缩公差，硬包装（彩盒）无公差
3. **定制箱子** — 剩余产品过多时建议定制箱尺寸
4. **多箱型组合** — 可同时使用多种箱型

## 参考数据（来自xlsx模板）

### 产品SKU（5个）

| SKU | 产品名 | 外箱尺寸(in) | 外箱尺寸(cm) | 数量 | 包装 |
|-----|--------|-------------|-------------|------|------|
| BR-ICE-DRAGON-FBA | 冰龙积木(1646片) | 17×13×3.2 | 43.2×33.0×8.5 | 72 | 硬 |
| BR-STEAM-SUB-FBA | 蒸汽朋克潜艇 | 13×9×4 | 33.0×22.9×10.2 | 35 | 硬 |
| MOC-Double-SafeLocker | 双层保险柜 | 5×4×1 | 12.7×10.2×2.5 | 60 | 软 |
| MOC-Heavy-SafeLocker | 重型保险柜 | 7×5×1 | 17.8×12.7×2.5 | 60 | 软 |
| MOC-Single-SafeLocker | 单层保险柜 | 6×4×1 | 15.2×10.2×2.5 | 25 | 软 |

### 箱子类型（模板中用到的-未经过验证，是通过彩盒尺寸估算）

| 箱号范围 | 外径(in) | 外径(cm) | 内径(壁厚1in) | 每箱内容 |
|---------|---------|---------|--------------|---------|
| B1-B12(12箱) | 19×13×18 | 48.3×33.0×45.7 | 17×11×16 | 冰龙6件/箱 |
| B13-B17(5箱) | 13×15×18 | 33.0×38.1×45.7 | 11×13×16 | 4SKU混装36件/箱 |

### 混装明细（B13-B17每箱）
- BR-STEAM-SUB-FBA: 7件 × 468in³ = 3,276in³
- MOC-Double-SafeLocker: 12件 × 20in³ = 240in³
- MOC-Heavy-SafeLocker: 12件 × 35in³ = 420in³
- MOC-Single-SafeLocker: 5件 × 24in³ = 120in³
- **每箱总体积**: 4,056in³ (vs 箱容3,510in³ → 115.5%，说明实际可挤压或测量有误差)

### 用户常用箱型（中国市场）
- 60×60×60cm（内径58×58×58）
- 60×50×50cm（内径58×48×48）
- 60×40×50cm（内径58×38×48）

## 技术选型

**纯前端 Web 应用（HTML/CSS/JavaScript）**
- 打开 HTML 文件即用，无后端依赖
- CDN：Three.js（3D可视化）、SheetJS（xlsx导入导出）
- 中文界面

## 核心数据模型

```javascript
// SKU / 产品
SKU {
  id: string,
  name: string,
  dimensions: { length, width, height }, // cm
  quantity: number,
  packagingType: 'hard' | 'soft',
  softTolerance: number,  // 0-20%，软包装压缩公差
}

// 箱子类型
BoxType {
  id: string,
  name: string,
  external: { length, width, height }, // cm
  wallThickness: number,  // 默认1cm
  get internal() { /* 外径-壁厚×2 */ }
}

// 混装组 — 核心概念
MixedGroup {
  id: string,
  name: string,
  boxTypeId: string,
  boxCount: number,         // 箱数（≥5）
  assignments: [            // 每组里面有哪些SKU、每箱各装多少
    { skuId: string, qtyPerBox: number }
  ],
  packagingType: 'hard' | 'soft' | 'mixed',
  get totalPerBox(): number        // 每箱总件数
  get totalAllBoxes(): number      // 所有箱总件数
}
```

## 核心算法：混装可行性验证

混装从简单的"同款装箱"变成了更复杂的"多种产品装一箱"。采用混合策略：

### 阶段1：快速体积拒绝
```
箱内体积 = 内长×内宽×内高
产品总体积 = Σ(每箱数量 × SKU体积)
if 产品总体积 > 箱内体积 → IMPOSSIBLE
```

### 阶段2：层架算法 (Shelf Level Packing)
```
1. 将每个SKU展开为单个单位，按高度降序排列
2. 同一高度的产品放在同一"层"（shelf）
3. 每层内用2D矩形排布算法（Best Short Side Fit）放置
4. 层层堆叠，总高度不超过箱高
5. 尝试箱子的6种方向（哪个边当高）

输出：VERIFIED FIT + 每层布局 + 3D可视化数据
```

### 阶段3：体积估算降级
```
当层架算法失败时，使用经验 packing factor：
- 单SKU硬包装：0.90
- 单SKU软包装：0.95
- 混装硬包装：0.65
- 混装软包装：0.75
- 混装硬+软：0.70

if 产品总体积 ≤ 箱内体积 × factor → ESTIMATED FIT (黄色警告)
else → WONT FIT
```

### 软包装公差处理
```
scale = (1 - tolerance)^(1/3)
有效尺寸 = 原始尺寸 × scale
有效体积 = 原始体积 × (1 - tolerance)
```

例如公差10%：每个维度×0.965，有效体积为原体积的90%。
混装场景中：先装硬包装产品，软包装产品塞入剩余空间。

## 混装组UI设计

用户通过"混装组"来定义装箱方案：

```
=== 混装组管理 ===
[新建混装组]

┌─ 混装组: 主力产品 ═══════════════════════════┐
│  选择箱型: [60×60×60cm        ▼]              │
│  箱数: [6] 箱  (≥5免配置费)                   │
│                                              │
│  SKU分配:                                     │
│  ┌─────────────────────────────────────────┐  │
│  │ 添加SKU: [选择SKU...          ▼] [+添加]  │  │
│  │                                          │  │
│  │ SKU           包装  公差  每箱数量  总计   │  │
│  │ 冰龙积木       硬   —       12      72   │  │
│  │ 潜艇积木       硬   —        7      35   │  │
│  │ ×删除                                        │
│  │ 每箱合计: 19件 | 总体积: xxx cm³           │  │
│  └─────────────────────────────────────────┘  │
│                                              │
│  验证结果: [✅ VERIFIED FIT 利用率87%]  [详细]  │
│                                              │
│  [删除此组]    [3D预览]                       │
└──────────────────────────────────────────────┘
```

三种分配模式：
1. **指定每箱数量**（最常用）：设SKU→每箱几个→选箱数→自动计算总件数
2. **自动均分**：输入各SKU总数量→设箱数→系统自动计算每箱数量
3. **逐箱编辑**：高级模式，每箱单独设定

## 同款装箱算法（单SKU场景）

针对一个SKU装一箱的简单场景，保留简单算法：

```
for 6种摆放方向:
  每边数量 = floor((内径 - 间隙) / (产品尺寸 + 间隙))
  每箱装 = 长边×宽边×高边
选每箱装最多的方向（数量相同时选利用率高的）
```

## 定制箱子建议

当剩余产品较多时触发：
```
遍历可能的摆放网格（Nx×Ny×Nz），约束每边≤60cm
寻找利用率最高的箱子尺寸
输出：建议外径尺寸、每箱装几个、共需几箱、利用率
```

## UI整体布局

```
+----------------------------------------------------------+
| FBA 装箱优化工具                   单位:[cm] 语言:[中文]  |
+----------------------------------------------------------+
| 第一步：SKU管理                           [导入xlsx]     |
| ┌─────────────────────────────────────────────────────────┐|
| | SKU名   长  宽  高  数量  包装类型  公差%  | 操作     | |
| | sku1    x   x   x   x       硬      —     | [删除]   | |
| | sku2   x   x   x   x        硬      —     | [删除]   | |
| | ...                                       |          | |
| | [+] 添加SKU                               |          | |
| └─────────────────────────────────────────────────────────┘|
|                                                           |
| 第二步：箱子类型                                           |
| ┌─────────────────────────────────────────────────────────┐|
| | 箱型名        外径(L×W×H)  壁厚  内径(自动)   | 操作   | |
| | 60×60×60     60×60×60    1.0   58×58×58    | [删除]  | |
| | 60×50×50     60×50×50    1.0   58×48×48    | [删除]  | |
| | [+] 添加箱子类型                                        | |
| └─────────────────────────────────────────────────────────┘|
|                                                           |
| 第三步：混装组配置                                         |
| [新建混装组] [自动分配方案]                                |
| ┌─ 混装组1: 冰龙单独装 ──────────────────────────────────┐|
| | 箱型60×60×60 ×6箱 | 冰龙72件(12/箱) | ✅ 验证通过      | |
| | [3D预览] [编辑] [删除]                                 | |
| └─────────────────────────────────────────────────────────┘|
| ┌─ 混装组2: 保险柜混装 ──────────────────────────────────┐|
| | 箱型60×50×50 ×5箱 | 4个SKU混装 | ✅ 验证通过           | |
| | [3D预览] [编辑] [删除]                                 | |
| └─────────────────────────────────────────────────────────┘|
|                                                           |
| 第四步：结果                            [导出xlsx] [打印]  |
| ┌─────────────────────────────────────────────────────────┐|
| | 总箱数: 11 | 已分配: 252/252 | 剩余: 0                | |
| | 定制建议: 无                                          | |
| | [所有箱子3D总览]                                        | |
| └─────────────────────────────────────────────────────────┘|
+----------------------------------------------------------+
```

## 3D可视化

Three.js 渲染，重点展示混装效果：
- 箱子半透明线框 + 不同SKU不同颜色的产品块
- 可切换查看每个箱子的内部布局（层架结构）
- OrbitControls 旋转/缩放
- "全部预览"模式展示所有箱子

## xlsx导入导出

- **导入**：解析Amazon模板 → 提取SKU列表和数量 → 用户补充尺寸和包装类型
- **导出**：将混装组方案写回标准Amazon模板格式，可上传到Seller Central

## 实施顺序

### 第1步：核心算法
- algorithm.js：6方向枚举、单SKU装箱、内径/间隙计算
- algorithm-mixed.js：混装层架算法（2D shelf packing）、体积估算降级、软包装公差
- distribution.js：多箱分配、自动均分
- customBox.js：定制箱子建议搜索

### 第2步：数据模型与配置
- config.js：常量（默认间隙0.5cm、壁厚1cm、最大边长60cm）、i18n中文
- 数据结构：SKU、BoxType、MixedGroup、PackingResult

### 第3步：基础UI
- index.html + style.css：布局、表单、结果展示
- ui.js：SKU管理、箱型管理、混装组配置、验证结果展示

### 第4步：3D可视化
- renderer3d.js：Three.js场景、层架渲染、SKU颜色编码

### 第5步：xlsx处理
- xlsxHandler.js：解析和写入Amazon模板

### 第6步：高级功能
- 替代方案对比展示
- localStorage保存配置
- 混装组自动推荐

## 验证方法

1. 输入60×60×60cm箱子，产品44x33.3x8.2cm，72套 → 验证单SKU装箱计算
2. 模拟xlsx混装场景：5个SKU分两个混装组 → 验证层架算法输出
3. 软包装产品设10%公差 → 验证有效尺寸缩小
4. 剩余产品较多时 → 验证定制箱子建议触发
5. 箱数<5时 → 验证警告提示
6. 打开index.html直接操作全部流程
