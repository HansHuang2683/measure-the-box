# 固定大硬彩盒后的剩余空间补货推荐功能实施计划

## 1. 功能目标

在用户已经确定一个装箱方案后，系统根据当前箱型、大硬彩盒布局和箱数，计算箱内真实剩余空腔，并推荐客户还可以追加订购多少小彩盒或软包，从而提高整箱空间利用率。

一句话定义：

> 固定现有大硬彩盒不移动，在真实剩余空腔中反推可追加的小件 SKU 数量、每箱数量、总订购数量和预计利用率提升。

## 2. 当前算法判断

现有项目已经具备以下基础：

- `algorithm-mixed.js`：可将混装组展开成单件产品，使用层架算法生成布局。
- `algorithm-cavity.js`：可从已放置产品中生成空腔，并把软包装填入低位空腔。
- `renderer3d.js`：可在 3D 面板中显示产品清单、箱内包围盒尺寸和剩余长宽高。
- `customBox.js`：可基于所有 SKU 反推定制箱方案。

但现有算法还不能直接满足本功能：

- 现有空腔算法是“已有软包尽量放进去”，不是“反推应采购多少小件”。
- 现有自动分配只少量试探软包数量，不追求最大化。
- 3D 面板里的“箱内剩余 长/宽/高”是整体包围盒剩余，不是真实可用空腔。
- 定制箱算法是业务启发式反推箱型，不是固定大硬彩盒后的剩余空间补货优化。

因此需要新增一个独立模块：`replenishment.js`。

## 3. 前端显示位置

### 3.1 主入口位置

放在第四步「结果」里的 `3D 可视化` 区域。

具体位置：

- 在 3D 控制栏中，放在「空腔吸附: OFF」按钮后面。
- 新增按钮：`补货推荐`
- 只有当用户已选择一个 3D 预览箱子时按钮可用。

目标布局：

```text
3D 可视化
[箱子下拉选择] [刷新] [沙盘模式] [空腔吸附: OFF] [补货推荐]
[3D 视图]
[右侧产品清单面板]
```

### 3.2 推荐结果显示位置

推荐结果显示在 3D 视图下方，作为一个独立面板，不放进右侧产品清单里。

原因：

- 右侧产品清单跟随 3D 画布，空间较窄。
- 补货推荐需要展示候选 SKU、每箱数量、总订购数量、利用率提升和风险提示。
- 独立面板更适合后续加“应用到混装组”“导出补货建议”等操作。

面板 DOM 建议：

```html
<div id="replenishmentPanel" class="replenishment-panel" style="display:none;"></div>
```

放置位置：

- 在 `#viewer-container` 后面。
- 在 `#cavityDiagnostics` 前面或后面均可，推荐放在 `#viewer-container` 后、`#cavityDiagnostics` 前。

目标结构：

```text
3D 可视化
  viewer-controls
  viewer-container
  replenishmentPanel
  cavityDiagnostics
```

### 3.3 推荐面板内容

面板顶部：

```text
剩余空间补货推荐
当前箱型：60×40×50
当前箱数：14 箱
当前利用率：73%
推荐后预计利用率：88%
```

候选小件输入区：

```text
候选小件来源：
[ ] 使用当前未分配 SKU
[ ] 手动添加候选小彩盒

候选列表：
SKU 名称 | 长 | 宽 | 高 | 包装 | 压缩公差 | 优先级 | 最大可推荐数量
```

结果区：

```text
推荐采购：
SKU                     每箱建议   14箱合计   利用率贡献   摆放区域
MOC-Double-SafeLocker   6 个/箱    84 个      +7.8%        侧边低位空腔
MOC-Single-SafeLocker   3 个/箱    42 个      +4.1%        顶部平台空腔

剩余不可用空间：
- 0.7cm 侧缝过窄，无法放入候选小件
- 顶部 5.0cm 空间可放软包，不建议放硬盒
```

操作按钮：

```text
[重新计算] [显示推荐件预览] [应用到当前混装组] [导出补货建议]
```

第一版只做 `[重新计算]` 和 `[显示推荐件预览]`，后两项放到第二版。

## 4. 数据模型

### 4.1 候选小件 SKU

```javascript
ReplenishmentCandidate {
  id: string,
  name: string,
  dimensions: { length, width, height },
  packagingType: 'hard' | 'soft',
  softTolerance: number,
  maxQty: number,
  priority: number,
  allowStackOnHard: boolean,
}
```

默认来源：

- 当前 SKU 表中未完全分配的 SKU。
- 用户手动新增的候选小件。

### 4.2 推荐结果

```javascript
ReplenishmentPlan {
  groupId: string,
  boxTypeId: string,
  boxCount: number,
  currentUtilization: number,
  projectedUtilization: number,
  additions: [
    {
      candidateId: string,
      name: string,
      qtyPerBox: number,
      totalQty: number,
      volumeContribution: number,
      placements: Array,
      placementSummary: string,
      warnings: string[],
    }
  ],
  cavitiesBefore: Array,
  cavitiesAfter: Array,
  unusableReasons: string[],
}
```

### 4.3 虚拟补货件

补货推荐计算时不直接改 `mixedGroups`，先创建虚拟产品：

```javascript
VirtualItem {
  skuId: string,
  skuName: string,
  dims: effectiveDims,
  originalDims: originalDims,
  packagingType: 'hard' | 'soft',
  candidateId: string,
  virtual: true,
}
```

## 5. 核心算法设计

### 5.1 输入

```javascript
generateReplenishmentPlan({
  group,
  boxType,
  skus,
  candidates,
  useCavityLayout: true,
  maxPerSkuPerBox,
})
```

### 5.2 固定当前大件布局

第一步先生成当前箱子的布局：

```javascript
const baseResult = generateMixedLayoutCavity(group, skus, boxType.internal);
```

然后从 `baseResult.layers` 中提取已放置产品：

```javascript
const placedItems = extractPlacedItemsFromLayers(baseResult.layers);
```

第一版默认锁定所有已放置产品，不移动、不重新排序。

### 5.3 构建真实空腔

不要使用 3D 面板里的包围盒剩余尺寸。

必须使用：

```javascript
const cavities = buildCavitiesFromPlaced(placedItems, boxL, boxW, boxH);
```

并过滤不可用空腔：

- 任意边小于 `0.5cm` 的空腔过滤。
- 体积小于 `1cm³` 的空腔过滤。
- 硬包装候选不能放入支撑不足的悬空空腔。
- 软包装可允许 50% 以上支撑，但需要风险提示。

### 5.4 枚举候选放置

对每个候选 SKU：

- 软包装先调用 `getEffectiveDimensions`，考虑压缩公差。
- 硬包装使用原始尺寸。
- 枚举 6 种朝向。
- 对每个空腔计算网格容量：

```javascript
capacity = floor(cavity.l / orient.l)
         * floor(cavity.w / orient.w)
         * floor(cavity.h / orient.h)
```

每个候选生成多个放置方案：

```javascript
CandidateFit {
  candidateId,
  cavityId,
  orientation,
  capacity,
  usedVolume,
  fillRatio,
  supportOk,
  score,
}
```

### 5.5 优化目标

第一版采用贪心优化，后续可替换成动态规划或局部搜索。

评分函数：

```text
score =
  体积贡献 * 1000
  + 空腔填充率 * 300
  + 低位优先 * 200
  + SKU优先级 * 100
  + 贴边/支撑奖励
  - 碎片化惩罚
  - 硬包装悬空惩罚
```

排序后逐个尝试放置，放置成功后更新空腔列表，再继续计算。

### 5.6 数量反推

每箱推荐数量：

```javascript
qtyPerBox = placements.length
```

总推荐订购量：

```javascript
totalQty = qtyPerBox * group.boxCount
```

如果候选设置了 `maxQty`：

```javascript
totalQty = Math.min(totalQty, maxQty)
qtyPerBox = Math.floor(totalQty / group.boxCount)
```

第一版只推荐每箱固定数量，避免逐箱差异导致 FBA 表格复杂。

### 5.7 利用率计算

当前利用率：

```javascript
currentUtilization = baseProductVolume / boxVolume
```

推荐后利用率：

```javascript
projectedUtilization =
  (baseProductVolume + recommendedAddedVolume) / boxVolume
```

注意：

- 软包装使用有效体积参与计算。
- 面板同时展示“体积利用率”和“布局验证状态”。

## 6. 文件改动计划

### 6.1 新增文件

`js/replenishment.js`

职责：

- 从当前布局提取已放置产品。
- 构建真实空腔。
- 枚举候选小件放置。
- 生成补货推荐计划。
- 输出推荐件的虚拟 placements，供 3D 预览使用。

主要函数：

```javascript
function generateReplenishmentPlan(group, boxType, skus, candidates, options)
function buildReplenishmentCandidates(skus, mixedGroups)
function enumerateCandidateFits(candidate, cavities, boxInternal)
function scoreReplenishmentFit(fit, candidate, cavity, boxInternal)
function simulateReplenishmentPlacements(basePlaced, candidates, cavities, boxInternal)
function summarizeUnusableCavities(cavities, candidates)
```

### 6.2 修改 `index.html`

新增脚本：

```html
<script src="js/replenishment.js"></script>
```

位置：

- 放在 `algorithm-cavity.js` 后。
- 放在 `renderer3d.js` 和 `ui.js` 前。

新增按钮：

```html
<button class="btn btn-sm btn-outline" id="replenishmentBtn" onclick="toggleReplenishmentPanel()">补货推荐</button>
```

新增面板：

```html
<div id="replenishmentPanel" style="margin-top:12px;display:none;"></div>
```

### 6.3 修改 `js/ui.js`

新增 UI 控制函数：

```javascript
function toggleReplenishmentPanel()
function renderReplenishmentPanel()
function recalcReplenishment()
function getCurrentViewerGroupAndBox()
function buildManualCandidateFromForm()
function previewReplenishmentInViewer()
```

联动逻辑：

- 选择不同 3D 箱子时，清空或刷新补货推荐面板。
- 当前没有 viewer 选择时，按钮点击提示“请先选择一个箱子预览”。
- 切换空腔算法后，补货推荐重新计算。

### 6.4 修改 `js/renderer3d.js`

新增可选渲染推荐件：

```javascript
function loadGroupIntoViewer(group, boxType, result, boxIndex, cavities, overlayItems)
```

推荐件显示方式：

- 使用半透明绿色或虚线边框。
- 不改变原有产品颜色。
- 面板中标注为“推荐追加”。

第一版也可以不改函数签名，新增：

```javascript
window.renderReplenishmentOverlay = function(placements) {}
window.clearReplenishmentOverlay = function() {}
```

推荐使用 overlay 独立 group，避免污染当前布局。

### 6.5 修改 `css/style.css`

新增样式：

```css
.replenishment-panel {}
.replenishment-toolbar {}
.replenishment-candidate-table {}
.replenishment-result-table {}
.replenishment-score {}
.replenishment-warning {}
```

视觉原则：

- 不做大卡片套卡片。
- 面板作为 3D 区域下方的工具区。
- 推荐结果用紧凑表格，便于比较。

## 7. 前端交互流程

### 7.1 用户流程

1. 用户完成 SKU、箱型、混装组配置。
2. 用户点击“验证所有混装组”。
3. 用户在 3D 可视化下拉框选择一个箱子。
4. 用户点击“补货推荐”。
5. 面板展开，默认加载“当前未分配 SKU”作为候选。
6. 用户可手动新增小彩盒候选。
7. 点击“重新计算”。
8. 系统输出每箱推荐数量和总订购数量。
9. 用户点击“显示推荐件预览”，3D 中叠加显示推荐追加产品。

### 7.2 第一版不做

- 不自动修改混装组。
- 不自动加入 SKU 表总数量。
- 不支持逐箱不同数量。
- 不做服务端保存。
- 不把推荐结果写入 xlsx。

这些放到第二版。

## 8. 验收标准

### 8.1 算法验收

- 固定大硬彩盒布局后，推荐计算不得移动已有产品。
- 推荐的小件不得超出箱体边界。
- 推荐的小件不得与已有产品重叠。
- 硬包装小件不得放到支撑不足的悬空位置。
- 软包装可进入低位/侧边/顶部空腔，但必须标出压缩和支撑风险。
- 输出每箱数量、总订购数量和预计利用率提升。

### 8.2 UI 验收

- 3D 控制栏有“补货推荐”按钮。
- 3D 视图下方出现补货推荐面板。
- 没选择箱子时不能计算，并给出明确提示。
- 推荐结果可读，至少包含 SKU、每箱建议、总订购数量、利用率贡献和风险。
- “显示推荐件预览”能在 3D 中叠加显示推荐件，且能清除。

### 8.3 回归验收

- 原有 SKU 管理、箱型管理、混装组验证不受影响。
- 原有 3D 预览不受影响。
- 空腔吸附开关仍能正常切换。
- xlsx 导入导出不受影响。

## 9. 开发顺序

### 第一步：算法模块

- 新增 `js/replenishment.js`。
- 复用 `extractPlacedItemsFromLayers`、`buildCavitiesFromPlaced`、`subtractFromCavities`。
- 如果这些函数当前没有挂到 `window`，需要导出给新模块使用。
- 实现候选枚举、贪心放置、利用率计算。

### 第二步：基础 UI

- 在 3D 控制栏新增“补货推荐”按钮。
- 新增 `replenishmentPanel`。
- 默认候选来源使用当前未分配 SKU。
- 实现“重新计算”。

### 第三步：手动候选

- 在面板中支持手动添加候选小彩盒。
- 字段：名称、长、宽、高、包装类型、压缩公差、最大数量、优先级。

### 第四步：3D 叠加预览

- 推荐件使用独立 overlay group。
- 原有产品保持原色。
- 推荐件用绿色半透明材质和更高边框透明度。

### 第五步：结果应用和导出

- 第二版实现“应用到当前混装组”。
- 第二版实现“导出补货建议”。
- 第二版考虑把推荐结果写入 xlsx 的新 sheet。

## 10. 关键风险

- 3D 装箱是 NP-hard 问题，第一版采用启发式贪心，不承诺数学全局最优。
- 当前空腔切割可能产生重叠空腔，需要在模拟放置后继续做碰撞校验。
- 包围盒剩余尺寸不能用于推荐，只能作为用户理解的辅助信息。
- 软包装压缩不能只看体积，需要限制单边压缩和支撑面。
- 硬包装小件如果作为填充件，必须比软包装更保守。

## 11. 推荐第一版实现边界

第一版目标不要贪大：

- 只针对当前选中的一个混装组和箱型。
- 固定已有产品布局。
- 默认候选为未分配 SKU + 手动候选。
- 每箱推荐同样数量。
- 结果只展示和 3D 预览，不自动改业务数据。

这样可以最快验证核心价值：用户能看到“这个箱子还适合补多少小件”，并判断是否值得真的采购。
