# 第4步全量产品定制箱方案需求文档

# 第4步「全量产品定制箱方案」算法优化需求文档

## 1. 需求重新定义

第四步原本的「总箱数已分配」「剩余混装组」等结果可以保留，它们本身没有问题。

但是需要新增或改造一个模块：**全量产品定制箱方案**。

这个模块的定位不是：

```
剩余产品定制箱方案
```

也不是：

```
在已有箱型里，想办法把剩余产品塞进去
```

而是：

```
完全不使用已有箱型，不参考已经提供的箱子类型，重新基于所有产品的尺寸和总数量，反推出一套最优的定制箱方案。
```

也就是说，第四步最终应该同时给出两类信息：

1. 当前系统根据已有箱型 / 已有规则得到的装箱结果；
2. 额外给一个「如果我们重新定制箱子，应该怎么设计」的建议方案。

其中第二类才是本需求文档要优化的重点。

---

## 2. 核心业务理解

定制箱的意义是：

> 不再被现有箱型限制，而是根据产品尺寸、产品数量、硬包装 / 软包装属性，反向设计箱子的尺寸、箱数和装箱方式，从而让箱子体积尽可能最优。
> 

因此，定制箱方案不是为了处理“剩余产品”，而是为了重新分装**所有产品**。

换句话说：

```
已有箱型装箱结果 = 当前方案
全量产品定制箱方案 = 优化建议 / 替代方案 / 开发建议
```

定制箱方案应始终作为第四步结果中的一个独立建议模块输出，不管前面的混装结果是否成功、不管剩余混装组是否合理，都要给出这个建议。

---

## 3. 命名修改

原来的模块名称如果是：

```
剩余产品定制箱方案
```

需要改成：

```
全量产品定制箱方案
```

或：

```
定制箱优化建议方案
```

推荐最终标题：

```
全量产品定制箱优化方案
```

禁止继续使用：

```
剩余产品定制箱方案
```

因为这会让模型误以为只需要把剩余产品装起来。

---

## 4. 这个模块不应该做什么

### 4.1 不应该引用已有箱型

在这个模块里，不应该输出：

```
建议使用 A 箱
建议使用 B 箱
参考已有箱型 C
该产品可放入现有箱型 D
```

因为定制箱的前提是：

```
不用已有箱型，重新定制。
```

所以这个模块只应该输出：

```
推荐定制外箱尺寸
推荐每箱装入数量
推荐箱数
推荐摆放方向
推荐混装逻辑
推荐硬包装与软包装组合方式
```

---

### 4.2 不应该只处理剩余产品

不应该只分析：

```
剩余未装产品
剩余混装组
未分配 SKU
```

而应该分析：

```
所有 SKU
所有产品数量
所有产品尺寸
所有硬包装产品
所有软包装产品
```

这一步是重新设计装箱方案，不是补救剩余产品。

---

### 4.3 不应该强行凑“每箱同等数量”

有些情况下，不能简单要求每个箱子都装完全相同数量的产品。

例如：如果产品数量、尺寸和组合关系无法刚好平均分配，那么系统不应该为了凑整而输出不现实的方案。

正确逻辑是：

```
优先设计标准箱型
优先让大部分箱子保持同一装箱结构
允许最后一箱作为尾箱 / 非满箱 / 混装补足箱
```

也就是说，算法要区分：

```
标准箱
尾箱
混装补足箱
```

不能假设所有产品都能被完美拆成 5 个完全一致的箱子。

---

## 5. 定制箱方案的核心目标

全量产品定制箱方案的目标是：

```
通过所有 SKU 的产品尺寸和总数量，反推出需要多少箱、每种箱子多大、每箱装什么、怎么摆放，并尽可能做到体积最优。
```

优化目标按优先级排列：

```
1. 不使用已有箱型，完全按产品反推定制箱尺寸
2. 外箱最长边不超过 60cm
3. 优先满足硬包装产品的尺寸需求
4. 尽可能减少箱内无效空隙
5. 尽可能减少总箱数
6. 尽可能让大部分箱子采用统一箱型
7. 软包装产品优先作为填充件，利用硬包装箱型中的可设计冗余空间
8. 如果无法全部统一，则输出标准箱 + 尾箱 / 混装补足箱
```

---

## 6. 定制箱的核心设计原则

### 6.1 定制箱是反推出来的，不是选择出来的

已有箱型逻辑是：

```
已有一批箱子 → 判断产品能不能放进去
```

定制箱逻辑是：

```
已有一批产品 → 根据产品尺寸和数量反推出箱子尺寸
```

所以算法要从产品出发，而不是从箱子出发。

---

### 6.2 理论上追求零空隙，但实际要考虑内径 / 外径

定制箱的理想目标是：

```
产品装进去后，箱内没有明显多余空间，体积最优。
```

但是实际采购纸箱时通常看到的是外径，而产品使用的是内径，因此系统仍然需要保留最小必要余量。

推荐理解为：

```
理论装箱空间 = 产品真实占用空间
推荐定制外箱 = 理论装箱空间 + 内外径修正 + 最小装箱余量
```

不能为了“零空隙”而把外箱尺寸直接写成产品尺寸，否则实际产品可能装不进去。

建议默认规则：

```
硬包装产品：每个方向保留约 0.5-1.5cm 内部余量
软包装产品：可按可压缩逻辑处理，余量可以更小
纸箱外径：在内径需求基础上额外增加纸箱厚度修正
```

---

### 6.3 硬包装产品决定主箱型

硬包装产品通常：

```
尺寸大
不能压缩
不能强行挤压
必须优先满足底面尺寸和高度尺寸
```

因此定制箱算法应先处理硬包装产品。

硬包装产品有两种定制方式：

1. **单 SKU 专用定制箱**
    - 适合尺寸大、数量较多、难以混装的硬包装 SKU；
    - 箱子完全围绕这个 SKU 的尺寸和数量设计。
2. **硬包装主导混装定制箱**
    - 先满足硬包装 SKU 的底面 / 高度需求；
    - 再看是否能设计几厘米的冗余空间；
    - 这些冗余空间用于放软包装产品。

---

### 6.4 软包装产品不是主导箱型，而是填充变量

软包装产品通常：

```
尺寸小
厚度薄
可轻微压缩
可以填充侧边、顶部、夹层空间
```

例如本案例中软包装产品厚度约为：

```
2.5cm
```

这意味着：

```
只要硬包装主箱型中有几厘米空间，就可能塞入软包装产品。
```

但这个空间不是“随机剩余空间”，而应该在定制箱设计时被主动考虑。

例如：

```
硬包装产品刚好需要 43.2 × 33 × 51cm
如果软包装产品厚度是 2.5cm
可以考虑是否把某个方向设计为 35.5cm / 36cm
从而在侧边预留 2.5cm 左右的软包装填充空间
```

也就是说，定制箱方案可以主动设计少量冗余空间，用来吸收软包装产品，从而减少额外箱数。

---

## 7. 算法整体流程

### Step 1：读取所有 SKU 数据

系统必须读取所有 SKU，而不是只读取剩余 SKU。

每个 SKU 至少需要以下字段：

```
SKU 名称
总数量
单品长
单品宽
单品高 / 厚度
包装类型：硬包装 / 软包装
是否可压缩
是否易碎
是否可混装
单品重量，如有
```

如果缺少关键字段，需要标记：

```
该 SKU 缺少尺寸 / 数量 / 包装属性，当前定制箱方案为估算。
```

---

### Step 2：区分硬包装和软包装

先将 SKU 分成两类：

```
硬包装 SKU
软包装 SKU
```

硬包装 SKU 用于确定主箱型。

软包装 SKU 用于：

```
填充硬包装箱型的侧边空间
填充顶部空间
作为尾箱补足产品
必要时单独组成软包装混装箱
```

---

### Step 3：优先为硬包装 SKU 生成候选定制箱

对每个硬包装 SKU，枚举不同摆放方向。

假设产品尺寸为：

```
L = 长
W = 宽
H = 高 / 厚度
```

需要尝试：

```
L × W 作为底面，H 作为叠加方向
L × H 作为底面，W 作为叠加方向
W × H 作为底面，L 作为叠加方向
```

同时允许长宽方向旋转。

每个候选方案都要计算：

```
单箱可装数量
该 SKU 需要几箱
是否存在尾箱
外箱最长边是否 ≤ 60cm
箱内容积利用率
是否有可设计空间给软包装
```

---

### Step 4：反推单 SKU 专用箱

对于适合单独定制的硬包装 SKU，系统应反推：

```
每箱装几个
需要几箱
尾箱如何处理
推荐定制外箱尺寸
```

计算逻辑：

```
产品底面长 = A
产品底面宽 = B
产品叠加方向 = C
单箱装入数量 = N

理论内径长 = A
理论内径宽 = B
理论内径高 = C × N

推荐外箱长 = 理论内径长 + 安全余量 + 纸箱厚度修正
推荐外箱宽 = 理论内径宽 + 安全余量 + 纸箱厚度修正
推荐外箱高 = 理论内径高 + 安全余量 + 纸箱厚度修正
```

并校验：

```
max(推荐外箱长, 推荐外箱宽, 推荐外箱高) ≤ 60cm
```

---

### Step 5：反推硬包装主导混装箱

如果某个硬包装 SKU 的定制箱存在可利用方向，系统可以主动设计给软包装的空间。

这个逻辑不是：

```
箱子刚好设计完之后，有剩余就塞软包装
```

而是：

```
在不显著增加物流体积、不超过 60cm 的前提下，主动设计少量冗余空间给软包装产品。
```

例如：

```
硬包装产品底面：43.2 × 33cm
软包装产品厚度：2.5cm
```

可以尝试：

```
定制箱宽度 = 33cm + 2.5cm + 最小余量
```

这样软包装可以作为侧边填充件。

系统需要判断：

```
增加这 2.5-3cm 是否会导致最长边超过 60cm
增加这部分空间后是否能减少一个软包装单独箱
软包装是否可压缩塞入
是否会压坏硬包装产品
```

如果能减少箱数或提升整体体积利用率，则可以推荐这种硬包装主导混装箱。

---

### Step 6：处理软包装产品

软包装 SKU 的处理优先级：

```
1. 优先塞入硬包装主导定制箱的设计冗余空间
2. 如果无法全部塞入，再组成软包装专用混装箱
3. 如果数量很少，则作为尾箱补足件
```

软包装产品可以按以下方式处理：

```
按厚度叠放
按面积平铺
按可压缩程度估算
优先利用 2.5cm、5cm、7.5cm 这类厚度倍数空间
```

例如软包装产品厚度为 2.5cm：

```
预留 2.5cm 空间 ≈ 可放 1 层
预留 5cm 空间 ≈ 可放 2 层
预留 7.5cm 空间 ≈ 可放 3 层
```

---

### Step 7：生成箱型组合

系统最终需要输出：

```
箱型 A：单 SKU 专用定制箱
箱型 B：硬包装主导混装定制箱
箱型 C：软包装专用混装箱
箱型 D：尾箱 / 补足箱
```

但不一定每次都要有全部箱型。

算法应根据实际数据决定需要哪些箱型。

---

## 8. 箱数计算逻辑

对于每种箱型，都需要明确：

```
该箱型装哪些 SKU
每箱各装几个
共需要几箱
是否存在尾箱
尾箱装什么
```

例如：

```
某硬包装 SKU 总数量 = 26
每标准箱装 6 个
则：
标准箱 = 4 箱 × 6 个 = 24 个
尾箱 = 1 箱 × 2 个
```

尾箱不一定单独存在，可以进一步判断：

```
尾箱是否可以与软包装 SKU 混装
尾箱是否可以与其他硬包装 SKU 的尾数合并
是否值得重新调整单箱数量以减少尾箱
```

---

## 9. 方案选择优先级

多个定制箱候选方案之间，按以下优先级选择：

```
1. 总箱数更少
2. 总体积更小
3. 硬包装产品摆放更稳定
4. 大部分箱子装箱结构更统一
5. 尾箱更少
6. 软包装产品能被吸收到硬包装箱型中
7. 外箱最长边均不超过 60cm
8. 计算逻辑清晰，可落地采购
```

注意：

```
不能只为了减少箱数而把箱子设计得过大
不能只为了统一箱型而造成大量无效空间
不能只为了塞软包装而影响硬包装产品安全
```

---

## 10. 输出格式要求

第四步结果中，新增或替换为以下模块：

```markdown
## 全量产品定制箱优化方案

### 方案定位

本方案不使用已有箱型，也不基于剩余产品补救装箱，而是基于所有 SKU 的产品尺寸、总数量和包装属性，重新反推定制箱尺寸、箱数和混装方式。

### 定制箱方案汇总表

| 定制箱型 | 箱型定位 | 推荐外箱尺寸 | 主 SKU | 辅助 / 混装 SKU | 每箱装入结构 | 预计箱数 | 是否有尾箱 | 最长边是否 ≤60cm | 说明 |
|---|---|---|---|---|---|---|---|---|---|
| 定制箱 A | 单 SKU 专用箱 | XX × XX × XXcm | XXX | 无 | XXX × X个 | X箱 | 否 | 是 | 为硬包装 SKU 单独定制 |
| 定制箱 B | 硬包装主导混装箱 | XX × XX × XXcm | XXX | YYY 软包装 | XXX × X个 + YYY × Y个 | X箱 | 是 / 否 | 是 | 预留软包装填充空间 |
| 定制箱 C | 软包装混装箱 | XX × XX × XXcm | 无 | 多个软包装 SKU | A × X个 + B × Y个 | X箱 | 否 | 是 | 用于无法塞入硬包装箱的软包装 |
```

---

## 11. 每个箱型的详细输出格式

每个定制箱型必须输出：

```markdown
### 定制箱 A：XXX

- 箱型定位：单 SKU 专用箱 / 硬包装主导混装箱 / 软包装混装箱 / 尾箱
- 推荐外箱尺寸：
- 理论内径需求：
- 主 SKU：
- 辅助 / 混装 SKU：
- 每箱装入结构：
- 预计箱数：
- 是否存在尾箱：
- 摆放方向：
- 是否满足 60cm 单边限制：
- 为什么这样设计：
- 体积优化逻辑：
- 风险提示：
```

---

## 12. 示例：硬包装单 SKU 定制箱

### 输入

```
SKU：BR-ICE-DRAGON-FBA
总数量：6
产品尺寸：43.2 × 33 × 8.5cm
包装类型：硬包装
```

### 算法理解

采用平放叠加：

```
底面 = 43.2 × 33cm
叠加方向 = 8.5cm
6个叠放高度 = 8.5 × 6 = 51cm
理论内径需求 = 43.2 × 33 × 51cm
```

考虑最小余量和内外径修正：

```
推荐外箱约 = 44.5 × 34.5 × 53cm
```

### 输出

```markdown
### 定制箱 A：BR-ICE-DRAGON-FBA 专用箱

- 箱型定位：单 SKU 专用定制箱
- 推荐外箱尺寸：约 44.5 × 34.5 × 53cm
- 理论内径需求：43.2 × 33 × 51cm
- 主 SKU：BR-ICE-DRAGON-FBA
- 辅助 / 混装 SKU：无
- 每箱装入结构：BR-ICE-DRAGON-FBA × 6个
- 预计箱数：1箱
- 是否存在尾箱：否
- 摆放方向：平放叠加
- 是否满足 60cm 单边限制：满足，最长边约 53cm
- 为什么这样设计：
  - 该 SKU 为硬包装产品，尺寸较大，应优先满足其底面尺寸；
  - 6个产品叠放高度为 51cm；
  - 定制箱尺寸围绕该 SKU 反推，不使用已有箱型；
  - 外箱尺寸在理论内径需求基础上加入最小余量和纸箱厚度修正。
- 体积优化逻辑：
  - 箱子围绕该 SKU 精确设计，不保留大面积空余；
  - 该 SKU 可一箱装完，不需要尾箱。
- 风险提示：
  - 需确认供应商标注的是纸箱外径还是内径；
  - 若产品硬包装不能承压，需要增加缓冲空间；
  - 若增加缓冲空间后最长边超过 60cm，需要减少单箱数量。
```

---

## 13. 示例：硬包装主导 + 软包装填充定制箱

### 输入

```
硬包装 SKU：AAA
尺寸：43.2 × 33 × 8.5cm
软包装 SKU：BBB
尺寸：20 × 15 × 2.5cm
```

### 设计逻辑

硬包装产品决定基础箱型：

```
基础底面 = 43.2 × 33cm
```

软包装厚度为：

```
2.5cm
```

如果不超过 60cm，可以主动把箱子某一方向增加约 2.5-3cm：

```
定制箱宽度 ≈ 33 + 2.5 + 余量
```

让软包装产品作为侧边填充件放入。

### 输出

```markdown
### 定制箱 B：AAA 主导混装箱

- 箱型定位：硬包装主导混装定制箱
- 推荐外箱尺寸：约 XX × XX × XXcm
- 理论内径需求：硬包装占用空间 + 软包装侧边填充空间
- 主 SKU：AAA
- 辅助 / 混装 SKU：BBB
- 每箱装入结构：AAA × X个 + BBB × Y个
- 预计箱数：X箱
- 是否存在尾箱：视总数量而定
- 摆放方向：
  - AAA 平放叠加；
  - BBB 放入侧边 / 顶部预留空间。
- 是否满足 60cm 单边限制：是 / 否
- 为什么这样设计：
  - AAA 为硬包装 SKU，决定基础箱型；
  - BBB 为软包装 SKU，厚度仅 2.5cm，可轻微压缩；
  - 在不明显增加箱子体积、不超过 60cm 的前提下，主动设计侧边空间吸收 BBB。
- 体积优化逻辑：
  - 相比 BBB 单独成箱，该方案可以减少额外箱数；
  - 软包装被设计进硬包装箱的冗余空间，而不是随机塞入。
- 风险提示：
  - 需确认 BBB 是否可以被轻微挤压；
  - 不能造成外箱鼓包；
  - 不能压坏 AAA 的硬包装。
```

---

## 14. 禁止输出内容

禁止在「全量产品定制箱优化方案」中输出：

```
使用已有 A 箱
使用已有 B 箱
参考当前箱型
剩余产品建议定制箱
将未分配产品放入现有箱子
```

禁止只说：

```
建议定制箱子。
```

必须输出：

```
为什么定制
为哪些 SKU 定制
每箱装什么
每箱装几个
需要几箱
箱子尺寸是多少
是否超过 60cm
是否有尾箱
软包装如何被吸收
```

---

## 15. 开发实现要求总结

请开发实现时重点注意：

```
1. 该模块分析对象是所有 SKU，不是剩余 SKU
2. 该模块不使用已有箱型，不引用已有箱子类型
3. 该模块基于产品尺寸和总数量反推箱型
4. 硬包装 SKU 优先决定主箱型
5. 软包装 SKU 作为填充变量，用于侧边 / 顶部 / 尾箱空间
6. 定制箱追求体积最优，而不是简单把剩余产品装起来
7. 允许输出单 SKU 专用箱、硬包装主导混装箱、软包装混装箱、尾箱
8. 需要计算每种箱型的尺寸、装箱结构、箱数和尾箱情况
9. 需要校验最长边是否 ≤60cm
10. 不能强行要求所有箱子完全同结构，允许标准箱 + 尾箱
11. 如果软包装厚度较小，例如 2.5cm，应尝试设计 2.5cm / 5cm / 7.5cm 等倍数空间吸收软包装
12. 输出必须是可执行的采购 / 装箱建议，而不是泛泛建议
```

---

## 16. 一句话定义

这个模块的本质是：

```
不使用已有箱型，不只处理剩余产品，而是基于所有 SKU 的尺寸、数量和包装属性，反向设计全新的定制箱型组合，让硬包装产品决定主箱型，软包装产品作为填充变量，从而输出更优体积、更少箱数、更可执行的全量产品定制箱方案。
```